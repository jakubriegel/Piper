version: "3.7"
services:
  frontend:
    build:
      context: ../service/frontend
      target: ${TARGET}
      dockerfile: docker/${ARCHITECTURE}/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ../service/frontend/:/frontend/